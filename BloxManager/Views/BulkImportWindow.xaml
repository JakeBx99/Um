<Window x:Class="BloxManager.Views.BulkImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bulk Import Accounts" 
        Height="500" Width="600"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundMainBrush}"
        TextElement.Foreground="{StaticResource TextPrimaryBrush}"
        FontFamily="Segoe UI Variable, Segoe UI, Arial">
    
    <Window.Resources>
        <Geometry x:Key="IconClose">M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z</Geometry>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Bulk Import Accounts" FontSize="20" FontWeight="Bold" Margin="0,0,0,8"/>
            <TextBlock x:Name="InstructionText" Text="Separate the accounts with new lines" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
            <TextBlock x:Name="FormatText" Text="Format: user:pass" FontSize="12" Foreground="{StaticResource TextMutedBrush}"/>
        </StackPanel>

        <!-- Input Area -->
        <Border Grid.Row="1" Background="{StaticResource BackgroundSecondaryBrush}" 
                BorderBrush="{StaticResource GlassBorderBrush}" BorderThickness="1" CornerRadius="8">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <TextBox x:Name="InputTextBox" 
                         AcceptsReturn="True" 
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         FontFamily="Consolas" 
                         FontSize="12"
                         Padding="12"
                         Background="Transparent"
                         BorderThickness="0"
                         Foreground="{StaticResource TextPrimaryBrush}"
                         Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"/>
            </ScrollViewer>
        </Border>

        <!-- Status -->
        <TextBlock Grid.Row="2" x:Name="StatusText" 
                   Text="Ready to import accounts" 
                   FontSize="12" 
                   Foreground="{StaticResource TextMutedBrush}" 
                   Margin="0,12,0,16"
                   Visibility="{Binding StatusMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

        <!-- Actions -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" 
                    Style="{StaticResource SecondaryActionButton}" 
                    Margin="0,0,12,0"
                    Click="CancelButton_Click"/>
            <Button Content="Import Accounts" 
                    Style="{StaticResource PrimaryActionButton}" 
                    Command="{Binding ImportCommand}"/>
        </StackPanel>

        <!-- Loading Overlay -->
        <Grid Grid.RowSpan="4" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <Border Background="#A0000000"/>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="42" Height="42" Foreground="{StaticResource PrimaryAccentBrush}"/>
                <TextBlock Text="{Binding StatusMessage}" HorizontalAlignment="Center" Margin="0,16,0,0" Foreground="White"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
